[build]
  publish = "public"
  command = "git config core.quotepath false && git config core.precomposeunicode true && hugo build --gc --minify"

[build.environment]
  HUGO_VERSION = "0.158.0"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"

